---
import BaseLayout from "../layouts/BaseLayout.astro";

const aboutVideoSchema = {
  "@type": "VideoObject",
  "@id": "https://art.jonathanrreed.com/about/#corridor-video",
  name: "I Made Art That HACKS Your Eyes",
  description:
    "Reference video that inspired the Algorithmic Art Gallery exploration of generated visual systems and perception-driven artwork.",
  thumbnailUrl: "https://i.ytimg.com/vi/SxsN6FRXMWQ/hqdefault.jpg",
  embedUrl: "https://www.youtube.com/embed/SxsN6FRXMWQ",
  uploadDate: "2024-01-01",
  publisher: {
    "@type": "Organization",
    name: "Corridor Crew",
  },
};
---

<BaseLayout
  title="Generative Art Process | Jonathan R Reed"
  description="Learn how the Algorithmic Art Gallery by Jonathan R Reed creates generative art using space-filling curves and mathematical algorithms."
  structuredData={[aboutVideoSchema]}
>
  <div class="about-page">
    <!-- Header -->
    <header class="about-hero" data-reveal>
      <p class="mono-text about-eyebrow">Process</p>
      <h1 class="about-header">About</h1>
      <p class="mono-text about-meta">
        By Jonathan R Reed. Updated <time datetime="2026-04-21">April 21, 2026</time>.
      </p>
    </header>

    <div class="about-content">
      <!-- The Project -->
      <section class="about-section" data-reveal>
        <div class="section-pill">
          <span class="mono-text">01</span>
        </div>
        <h2 class="about-subheader">The Project</h2>
        <div class="about-card">
          <p>
            This is a small generative art project by me, Jonathan Reed! It uses
            space-filling curves (Hilbert, Morton, Peano) plus spiral and random-walk
            traversals, combined with color mapping to produce patterns. You control
            the seed, traversal curve, growth mode, symmetry, and color flow, then let
            the algorithm fill in the pixels.
          </p>
          <p>
            Every output is deterministic: same settings produce the same image.
            Change a parameter and you get something new. It's mostly an
            experiment in seeing what happens when you let math do the drawing.
          </p>
          <p>
            The project is also a practical study in creative tooling. The generator
            keeps the controls visible, the output repeatable, and the saved gallery
            close to the work surface so each image can be treated as a small
            experiment rather than a disposable random effect.
          </p>
        </div>
        <p class="about-link">
          <a
            href="https://jonathanrreed.com/projects/"
            target="_blank"
            rel="noopener noreferrer"
          >
            See more projects by Jonathan Reed →
          </a>
        </p>
      </section>

      <!-- Technology & Video -->
      <section class="about-section" data-reveal>
        <div class="section-pill">
          <span class="mono-text">02</span>
        </div>
        <h2 class="about-subheader">The Technology</h2>
        <div class="about-card">
          <p>
            Built with Astro and React for a fast, mostly-static experience, with
            the heavy pixel work offloaded to a Web Worker so the interface stays
            responsive while a piece plots. The interface borrows from drafting and
            plotter culture — warm paper, graphite ink, a single oxide plot-accent,
            and a real measurement grid — so the chrome stays quiet and the
            generated color does the talking.
          </p>
        </div>

        <div class="video-frame" data-reveal>
          <p class="mono-text video-eyebrow">Reference</p>
          <h3 class="video-title">I Made Art That HACKS Your Eyes</h3>
          <div class="video-container">
            <iframe
              width="560"
              height="315"
              src="https://www.youtube.com/embed/SxsN6FRXMWQ?si=p4sz9KDZs15OeUoL"
              title="YouTube video player"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
              referrerpolicy="strict-origin-when-cross-origin"
              allowfullscreen
            >
            </iframe>
          </div>
          <p class="video-caption">
            The video is included as a reference point for the visual idea, but the
            generator itself is built around browser-side controls, deterministic
            settings, and exportable studies that can be recreated from documented
            parameters.
          </p>
        </div>
      </section>

      <!-- How It Works -->
      <section class="about-section" data-reveal>
        <div class="section-pill">
          <span class="mono-text">03</span>
        </div>
        <h2 class="about-subheader">How It Works</h2>
        <div class="steps-grid">
          <div class="step-card">
            <div class="step-number mono-text">1</div>
            <h3>Select Parameters</h3>
            <ul class="parameter-list">
              <li>
                <strong>Seed & Growth</strong>: Control the seed value, pattern
                size, growth mode, seed shape, and more
              </li>
              <li>
                <strong>Color & Symmetry</strong>: Define the traversal curve, color
                progression, symmetry, and finishing effects
              </li>
            </ul>
          </div>
          <div class="step-card">
            <div class="step-number mono-text">2</div>
            <h3>Generate</h3>
            <p>
              The algorithm processes your parameters to plot a unique piece of algorithmic art.
            </p>
          </div>
          <div class="step-card">
            <div class="step-number mono-text">3</div>
            <h3>Export</h3>
            <p>
              Download your creation as a PNG or PDF file.
            </p>
          </div>
          <div class="step-card">
            <div class="step-number mono-text">4</div>
            <h3>Save</h3>
            <p>
              Add your favorite generations to your personal gallery to revisit later.
            </p>
          </div>
        </div>
      </section>

      <!-- Pattern Generation Options -->
      <section class="about-section" data-reveal>
        <div class="section-pill">
          <span class="mono-text">04</span>
        </div>
        <h2 class="about-subheader">Pattern Generation Options</h2>
        <p class="feature-intro">
          Click on any option below to learn more about it:
        </p>
        <div class="feature-grid">
          <div class="feature-item" data-feature="curve-types">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Curve Types</h3>
                <p class="feature-summary">Hilbert, Morton, Peano, Spiral, Random Walk</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>Hilbert:</strong> A space-filling curve that preserves locality
                  well, creating smoother color transitions and more organic patterns.
                </li>
                <li>
                  <strong>Morton:</strong> The Z-order curve interleaves coordinate bits,
                  creating blockier, quadrant-based color regions.
                </li>
                <li>
                  <strong>Peano:</strong> A recursive 3×3 space-filling curve — a denser,
                  more woven path through color space than Hilbert.
                </li>
                <li>
                  <strong>Spiral:</strong> Archimedean-spiral ordering from the center
                  outward, so color winds out in rings.
                </li>
                <li>
                  <strong>Random Walk:</strong> A constrained, DLA-style random walk
                  orders the palette for organic, unpredictable color flow.
                </li>
              </ul>
            </div>
          </div>
          <div class="feature-item" data-feature="growth-modes">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Growth Modes</h3>
                <p class="feature-summary">Crystal, Nebula, Rings, Flow</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>Crystal:</strong> Grows outward by distance from the center,
                  forming crystalline, radial structures.
                </li>
                <li>
                  <strong>Nebula:</strong> Near-random growth priority produces cloud-like,
                  organic shapes with diffused edges.
                </li>
                <li>
                  <strong>Rings:</strong> Priority follows a sine of the radius, generating
                  concentric rings and orbital bands.
                </li>
                <li>
                  <strong>Flow:</strong> Growth follows a deterministic flow field, so
                  structure streams along curving currents.
                </li>
              </ul>
            </div>
          </div>
          <div class="feature-item" data-feature="seed-shapes">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Seed Shapes</h3>
                <p class="feature-summary">Point, Dual, Circle, Line</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>Point:</strong> Starts growth from a single point at the
                  center, creating a radial expansion pattern.
                </li>
                <li>
                  <strong>Dual:</strong> Begins from two seed points for paired,
                  symmetrical growth.
                </li>
                <li>
                  <strong>Circle:</strong> Begins with a circular boundary that expands
                  outward, creating ring-like structures.
                </li>
                <li>
                  <strong>Line:</strong> Initiates growth from a horizontal line across
                  the center, creating mirror-like patterns.
                </li>
              </ul>
            </div>
          </div>
          <div class="feature-item" data-feature="symmetry">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Symmetry</h3>
                <p class="feature-summary">None, Bilateral, Quadrantal, Radial</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>None:</strong> No symmetry applied - pattern grows freely
                  in all directions without mirroring.
                </li>
                <li>
                  <strong>Bilateral:</strong> Mirror symmetry along a central axis,
                  like a butterfly's wings.
                </li>
                <li>
                  <strong>Quadrantal:</strong> Four-way symmetry, mirrored in four
                  quadrants from the center.
                </li>
                <li>
                  <strong>Radial:</strong> Eight-fold rotational symmetry radiating
                  from the center point.
                </li>
              </ul>
            </div>
          </div>
          <div class="feature-item" data-feature="color-progression">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Color Progression</h3>
                <p class="feature-summary">Sequential, Shuffled, Base Distance, Saturation, Brightness</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>Sequential:</strong> Colors follow a strict sequence based
                  on their position in the RGB space.
                </li>
                <li>
                  <strong>Shuffled:</strong> Colors are randomly arranged while maintaining
                  visual coherence.
                </li>
                <li>
                  <strong>Base Distance:</strong> Colors are distributed based on
                  their distance from a base color.
                </li>
                <li>
                  <strong>Saturation:</strong> Sorts the palette by saturation for an
                  AllRGB-style flow.
                </li>
                <li>
                  <strong>Brightness:</strong> Sorts the palette by brightness intensity.
                </li>
              </ul>
            </div>
          </div>
          <div class="feature-item" data-feature="finish">
            <div class="feature-header">
              <div class="feature-header-text">
                <h3>Finish &amp; Effects</h3>
                <p class="feature-summary">Gradient Map, Dithering, Anti-aliasing</p>
              </div>
              <span class="feature-expand-icon" aria-hidden="true">+</span>
            </div>
            <div class="feature-details">
              <ul>
                <li>
                  <strong>Gradient Map:</strong> Remaps the generated colors by brightness
                  through a curated palette — sunset, ocean, monochrome, neon, forest, or magma.
                </li>
                <li>
                  <strong>Dithering:</strong> Ordered (Bayer) dithering adds fine tonal
                  texture and softens color banding.
                </li>
                <li>
                  <strong>Anti-aliasing:</strong> Edge-preserving smoothing softens harsh
                  boundaries between color regions.
                </li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      <!-- Tech Stack -->
      <section class="about-section" data-reveal>
        <div class="section-pill">
          <span class="mono-text">05</span>
        </div>
        <h2 class="about-subheader">Tech Stack</h2>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>
            </div>
            <span class="tech-name">Astro</span>
            <span class="tech-role mono-text">Framework</span>
          </div>
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="14.31" y1="8" x2="20.05" y2="17.94"/><line x1="9.69" y1="8" x2="21.17" y2="8"/><line x1="7.38" y1="12" x2="13.12" y2="2.06"/><line x1="9.69" y1="16" x2="3.95" y2="6.06"/><line x1="14.31" y1="16" x2="2.83" y2="16"/><line x1="16.62" y1="12" x2="10.88" y2="21.94"/></svg>
            </div>
            <span class="tech-name">React</span>
            <span class="tech-role mono-text">UI Library</span>
          </div>
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z"/></svg>
            </div>
            <span class="tech-name">Tailwind</span>
            <span class="tech-role mono-text">Styling</span>
          </div>
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/></svg>
            </div>
            <span class="tech-name">Canvas API</span>
            <span class="tech-role mono-text">Renderer</span>
          </div>
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
            </div>
            <span class="tech-name">Web APIs</span>
            <span class="tech-role mono-text">Storage / Export</span>
          </div>
          <div class="tech-card">
            <div class="tech-icon" aria-hidden="true">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/><polyline points="13 2 13 9 20 9"/></svg>
            </div>
            <span class="tech-name">PDF / PNG</span>
            <span class="tech-role mono-text">Export Formats</span>
          </div>
        </div>
      </section>

      <!-- Technical Specifications -->
      <section class="about-section technical-specs" data-reveal>
        <div class="section-pill">
          <span class="mono-text">06</span>
        </div>
        <h2 class="about-subheader">Technical Specifications</h2>
        <ul class="spec-list">
          <li>
            <span class="spec-label">Pattern Sizes</span>
            <span class="spec-value">From 128×128 to 4096×4096 pixels</span>
          </li>
          <li>
            <span class="spec-label">Traversal curves</span>
            <span class="spec-value">Hilbert, Morton (Z-order), Peano, Spiral, Random Walk</span>
          </li>
          <li>
            <span class="spec-label">Growth modes</span>
            <span class="spec-value">Crystal, Nebula, Rings, Flow</span>
          </li>
          <li>
            <span class="spec-label">Export Options</span>
            <span class="spec-value">PNG and PDF formats</span>
          </li>
          <li>
            <span class="spec-label">Settings Storage</span>
            <span class="spec-value">Browser local storage for saved settings</span>
          </li>
        </ul>
      </section>
    </div>
  </div>
</BaseLayout>

<script is:inline>
  // Feature cards expand/collapse functionality
  (function () {
    if (window.__aagAbout) return;
    window.__aagAbout = true;
    document.addEventListener("astro:page-load", () => {
    const featureItems = document.querySelectorAll(".feature-item");
    if (!featureItems.length) return;

    featureItems.forEach((item) => {
      if (!(item instanceof HTMLElement)) return;

      // Set initial ARIA attributes
      item.setAttribute("role", "button");
      item.setAttribute("tabindex", "0");
      item.setAttribute("aria-expanded", "false");
      const dataFeature = item.getAttribute("data-feature") || "feature";
      const detailsId = `details-${dataFeature}`;
      const details = item.querySelector(".feature-details");
      if (details) {
        details.id = detailsId;
        item.setAttribute("aria-controls", detailsId);
      }

      // Add click and keyboard event handlers
      item.addEventListener("click", (e) => {
        addClickEffect(item);
        toggleFeature(item, featureItems);
      });
      item.addEventListener("keydown", (e) => {
        if (
          e instanceof KeyboardEvent &&
          (e.key === "Enter" || e.key === " ")
        ) {
          e.preventDefault();
          addClickEffect(item);
          toggleFeature(item, featureItems);
        }
      });
    });

    function addClickEffect(item) {
      if (!(item instanceof HTMLElement)) return;
      item.classList.add("click-effect");
      setTimeout(() => {
        item.classList.remove("click-effect");
      }, 150);
    }

    function toggleFeature(item, allItems) {
      if (!(item instanceof HTMLElement)) return;
      const wasActive = item.classList.contains("active");

      // Close all items
      allItems.forEach((otherItem) => {
        if (!(otherItem instanceof HTMLElement)) return;
        otherItem.classList.remove("active");
        otherItem.setAttribute("aria-expanded", "false");
        const otherIcon = otherItem.querySelector(".feature-expand-icon");
        if (otherIcon) otherIcon.textContent = "+";
      });

      // If the clicked item wasn't active before, make it active
      if (!wasActive) {
        item.classList.add("active");
        item.setAttribute("aria-expanded", "true");
        const expandIcon = item.querySelector(".feature-expand-icon");
        if (expandIcon) expandIcon.textContent = "−";
      }
    }
    });
  })();
</script>

<style>
  /* ========================================
     ABOUT — a process documentation sheet
     ======================================== */
  .about-page {
    width: min(960px, calc(100% - 2rem));
    margin: 1.5rem auto 2rem;
    padding: 0 1rem;
    box-sizing: border-box;
  }

  .about-hero {
    text-align: center;
    padding: 2.5rem 0 2.5rem;
  }

  .about-eyebrow {
    color: var(--accent-strong);
    font-size: 0.72rem;
    letter-spacing: 0.2em;
    margin-bottom: 0.75rem;
  }

  .about-header {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 480;
    color: var(--ink);
    margin: 0 0 0.75rem;
    letter-spacing: -0.03em;
    line-height: 0.95;
  }

  .about-meta {
    color: var(--ink-2);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: 3.25rem;
  }

  /* Sections with a drafting "plate" number */
  .about-section {
    position: relative;
    padding-left: 3.25rem;
  }

  .section-pill {
    position: absolute;
    left: 0;
    top: 0.4rem;
    width: 2.1rem;
    height: 2.1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--line-strong);
    background: var(--paper);
    color: var(--accent-strong);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.04em;
  }

  .about-subheader {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    font-weight: 470;
    color: var(--ink);
    margin: 0 0 1.25rem;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .about-card {
    background: var(--paper);
    border: 1px solid var(--line-strong);
    border-left: 3px solid var(--accent);
    padding: 1.5rem 1.75rem;
  }

  .about-card p {
    font-size: 0.95rem;
    line-height: 1.75;
    margin: 0 0 1rem;
    color: var(--ink-2);
  }

  .about-card p:last-child {
    margin-bottom: 0;
  }

  .about-link {
    margin-top: 1.5rem;
  }

  .about-link a {
    color: var(--ink);
    text-decoration: none;
    font-family: var(--font-mono);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.76rem;
    letter-spacing: 0.1em;
    display: inline-block;
    padding: 0.7rem 1.2rem;
    border: 1px solid var(--rule);
    background: var(--paper);
    transition: background var(--transition-fast), color var(--transition-fast);
  }

  .about-link a:hover {
    background: var(--ink);
    color: var(--table);
  }

  /* Video — a reference plate */
  .video-frame {
    margin-top: 2rem;
    background: var(--paper);
    border: 1px solid var(--line-strong);
    padding: 1.5rem;
  }

  .video-eyebrow {
    color: var(--accent-strong);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    margin-bottom: 0.5rem;
  }

  .video-title {
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 470;
    color: var(--ink);
    margin: 0 0 1rem;
    padding-left: 0.75rem;
    border-left: 3px solid var(--accent);
  }

  .video-container {
    position: relative;
    padding-bottom: 56.25%;
    height: 0;
    overflow: hidden;
    border: 1px solid var(--line-strong);
    background: var(--paper-2);
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  .video-caption {
    margin: 1rem 0 0;
    font-size: 0.9rem;
    line-height: 1.65;
    color: var(--ink-2);
  }

  /* Steps */
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1px;
    background: var(--line-strong);
    border: 1px solid var(--line-strong);
  }

  .step-card {
    background: var(--paper);
    padding: 1.5rem;
    transition: background var(--transition-fast);
  }

  .step-card:hover {
    background: var(--paper-2);
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border: 1px solid var(--line-strong);
    background: var(--paper-2);
    color: var(--accent-strong);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 0.85rem;
  }

  .step-card h3 {
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 470;
    color: var(--ink);
    margin: 0 0 0.5rem;
  }

  .step-card p {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--ink-2);
    margin: 0;
  }

  .parameter-list {
    list-style-type: none;
    padding-left: 0;
    margin: 0;
  }

  .parameter-list li {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--ink-2);
    margin-bottom: 0.5rem;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
  }

  .parameter-list li::before {
    content: "▸";
    color: var(--accent);
    flex-shrink: 0;
    font-size: 0.7rem;
    margin-top: 0.2rem;
  }

  /* Feature index (expandable) */
  .feature-intro {
    font-size: 0.95rem;
    color: var(--ink-2);
    margin-bottom: 1.25rem;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }

  .feature-item {
    background: var(--paper);
    border: 1px solid var(--line-strong);
    padding: 1.25rem;
    cursor: pointer;
    transition: border-color var(--transition-fast), background var(--transition-fast);
    position: relative;
  }

  .feature-item:hover {
    border-color: var(--accent);
  }

  .feature-item.click-effect {
    background: var(--paper-2);
  }

  .feature-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .feature-header-text {
    min-width: 0;
  }

  .feature-item h3 {
    font-size: 0.95rem;
    font-family: var(--font-mono);
    color: var(--ink);
    margin: 0 0 0.35rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .feature-summary {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    margin: 0;
    color: var(--ink-2);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .feature-expand-icon {
    color: var(--accent);
    font-size: 1.25rem;
    font-family: var(--font-mono);
    font-weight: 400;
    line-height: 1;
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }

  .feature-item.active .feature-expand-icon {
    color: var(--accent);
    transform: rotate(45deg);
  }

  .feature-details {
    max-height: 0;
    opacity: 0;
    overflow: hidden;
    transition: all 0.35s cubic-bezier(0.2, 0, 0, 1);
    margin-top: 0;
  }

  .feature-item.active {
    background-color: var(--paper-2);
  }

  .feature-item.active .feature-details {
    max-height: 600px;
    opacity: 1;
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px dashed var(--line-strong);
  }

  .feature-details ul {
    padding-left: 1rem;
    margin: 0;
    list-style-type: none;
  }

  .feature-details li {
    margin-bottom: 0.75rem;
    color: var(--ink-2);
    font-size: 0.85rem;
    line-height: 1.55;
    position: relative;
  }

  .feature-details li::before {
    content: "–";
    position: absolute;
    left: -1rem;
    color: var(--accent);
  }

  .feature-details li:last-child {
    margin-bottom: 0;
  }

  /* Tech stack */
  .tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
    gap: 1px;
    background: var(--line-strong);
    border: 1px solid var(--line-strong);
  }

  .tech-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.55rem;
    padding: 1.4rem 0.75rem;
    background: var(--paper);
    transition: background var(--transition-fast);
  }

  .tech-card:hover {
    background: var(--paper-2);
  }

  .tech-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border: 1px solid var(--line-strong);
    background: var(--paper-2);
    color: var(--accent);
  }

  .tech-name {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 470;
    color: var(--ink);
  }

  .tech-role {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    color: var(--ink-2);
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  /* Technical specs */
  .technical-specs {
    padding-bottom: 2rem;
  }

  .spec-list {
    list-style: none;
    padding: 0;
    margin: 0;
    border-top: 1px solid var(--line-strong);
  }

  .spec-list li {
    padding: 1rem 0;
    font-size: 0.85rem;
    font-family: var(--font-mono);
    border-bottom: 1px solid var(--line);
    color: var(--ink);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
  }

  .spec-list li:last-child {
    border-bottom: 1px solid var(--line-strong);
  }

  .spec-label {
    color: var(--ink);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    min-width: 200px;
    flex-shrink: 0;
  }

  .spec-value {
    color: var(--ink-2);
  }

  strong {
    color: var(--ink);
    font-weight: 600;
  }

  @media (max-width: 768px) {
    .about-section { padding-left: 0; }
    .section-pill { position: static; margin-bottom: 0.75rem; }
    .about-card { padding: 1.25rem; }
    .video-frame { padding: 1.25rem; }
    .steps-grid { grid-template-columns: 1fr; }
    .feature-grid { grid-template-columns: 1fr; }
    .tech-grid { grid-template-columns: repeat(2, 1fr); }
    .spec-label { min-width: auto; width: 100%; }
  }

  @media (max-width: 480px) {
    .about-header { font-size: 2.25rem; }
    .tech-grid { grid-template-columns: 1fr 1fr; }
  }
</style>
